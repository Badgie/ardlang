int[] array = {3,7,8,1,14}
int sum = 0
Button btn
LCD lcd

task incrTask() {
    if (btn.active()) {
        for (int i = 0; i < array.length(); i++) {
            array[i]++
        }
    }
}

task displaySum() {
    for (int j = 0; j < array.length(); j++) {
        sum = sum + array[j]
    }

    lcd.write(sum)
}
